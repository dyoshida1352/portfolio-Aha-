<div class="container">
	<h2 class="head-line mt-4 mb-4"><%= @user.name %>　のフォロワー</h2>
  <% if @users.exists? %>
    <%= render 'user_index', users: @users %>
  <% else %>
    <p>ユーザーはいません</p>
  <% end %>
</div>