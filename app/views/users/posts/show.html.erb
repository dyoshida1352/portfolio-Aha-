<div class="container">
  <h2 class="mt-4 mb-4"><%= @post.post_name %></h2>
  <div class="row d-flex">
    <h5 class="col-6">ユーザーネーム</h5>
    <div class="col-6">
    <% if @post.user == current_user %>
      <td><%= link_to "編集する", edit_users_post_path(@post), class:"btn btn-sm btn-success" %></td>
      <td><%= link_to "削除する", users_post_path(@post), method: :delete, "data-confirm" => "本当に消しますか？", class:"btn btn-sm btn-danger" %></td>
    <% end %>
    </div>
  </div>
  <div class="row">
    <div class="col-4">
      <div><%= attachment_image_tag @post, :post_image, fallback: "no_post_image.jpg", size: "250x200" %></div>
      <p>いいね<%= render "users/likes/like-btn", post: @post %></p>
      <p>コメント</p>
      <p>お気に入り</p>
      <p>タグ #〇〇　#〇〇</p>
    </div>
    <div class="col-6">
      <p>（アイデア説明）</p>
      <p><%= @post.post_description %></p>
    </div>
  </div>
  <h3 class="mt-3 mb-3 text-center">コメント一覧</h3>
  <%= render 'users/post_comments/comment', post: @post, post_comment: @post_comment %>
</div>
